### Call storage operations:
  Get every symbols with in the list by order:
    - Filter out every symbols that is already marked as documented
    Order them by importance:
        - The less proprietary functions call their is the bigger the function is independent